# Optional: HIBP API key for higher rate limits (free tier works without a key)
# Register at https://haveibeenpwned.com/API/Key
# HIBP_API_KEY=your_api_key_here

# Never commit a real .env file — this file is safe to commit
